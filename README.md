# test
practice

*text*

```js
function calculate(operation, num1, num2) {
  switch(operation) {
    case 'add':
      return num1 + num2;
    case 'sub':
      return num1 - num2;
    case 'mul':
      return num1 * num2;
    case 'div':
      return num1 / num2;
  }
}

```

![thor](tor.jpg "Marvel")